$(function() {
	tiy.init();
	window.router = new Router();
	Backbone.history.start();
});


// var svg = d3.select("svg");

// var eq = new Eq(svg);
// eq.start();

// React.render(React.createElement(ReactEQ, {play=true}),
	// document.body);